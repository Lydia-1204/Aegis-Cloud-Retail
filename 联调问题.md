# Aegis 系统联调问题记录

联调日期：2026-05-14  
联调范围：Go 微服务、Python AI 微服务、8080 临时网关、总部端/门店端前端真实模式

## 结论

本轮未发现阻塞性接口实现问题。REST、WebSocket、SSE、Go gRPC、Python gRPC、前端真实模式基础链路均通过联调测试。

## 环境与启动

| 项目 | 实际配置 |
| --- | --- |
| Go | go1.26.3 windows/amd64 |
| Python | conda 环境 `aegis`，Python 3.10.20 |
| Node.js | v22.16.0 |
| PostgreSQL | localhost:5432，`postgres/123456`，库 `aegis_go` / `aegis_python` |
| Go DB URL | `postgres://postgres:123456@localhost:5432/aegis_go?sslmode=disable` |
| Python DB URL | `postgresql+asyncpg://postgres:123456@localhost:5432/aegis_python` |
| Go gRPC 地址 | `localhost:50054` / `localhost:50055` |
| 前端真实模式 | `VITE_USE_MOCK=false` |
| 统一网关 | `http://localhost:8080/api` |

运行端口：

| 服务 | HTTP | gRPC |
| --- | --- | --- |
| foundation-data | 8081 | 50054 |
| store-ops | 8082 | 50055 |
| traffic-sense | 8083 | 50051 |
| ai-assistant | 8084 | 50053 |
| 临时网关 | 8080 | - |
| web-hq | 5173 | - |
| web-store | 5174 | - |

## 测试结果

| 测试项 | 结果 |
| --- | --- |
| 认证登录、`/api/auth/me`、修改密码负向校验 | 通过 |
| 门店、SKU、SKU 分类、用户列表/详情/新增/修改/停用 | 通过 |
| 销售流水新增/详情/修改/列表 | 通过 |
| 库存查询与盘点修正 | 通过 |
| 调拨单创建、下发、门店确认、异议、总部确认、作废 | 通过 |
| traffic-sense edge HTTP 与 WebSocket 实时客流 | 通过 |
| ai-assistant SSE 对话、会话列表、历史记录、debug-prompt | 通过 |
| foundation-data gRPC：`GetSkuDictionary`、`GetStoreContext` | 通过 |
| store-ops gRPC：`GetBusinessSnapshot`、`PushCustomerFlow`、`SyncInventoryDiagnosis`、`CreateAITransferOrder` | 通过 |
| traffic-sense gRPC：`GetRawCustomerFlow` | 通过 |
| ai-assistant gRPC：`GetAnalysisReport`、`GetSalesForecast` | 通过 |
| 前端总部端/门店端真实模式启动与 8080 网关配置 | 通过 |

自动化结果文件：`tmp/integration-test-results.json`

## 问题记录

| 编号 | 模块 | 严重程度 | 现象 | 结论 |
| --- | --- | --- | --- | --- |
| - | - | - | 未发现接口阻塞问题 | 本轮无需提交业务代码修复 |

## 注意事项

1. 本地直接运行 Python 微服务时，必须覆盖 Docker 风格的 Go gRPC 地址，使用 `localhost:50054` 和 `localhost:50055`。如果使用 `.env` 中的 `basic-data-center:50054` / `store-ops:50055`，本地直跑会导致 AI 到 Go 的 gRPC 调用失败并进入 mock/fallback。
2. 本轮联调未配置 `DEEPSEEK_API_KEY`，`ai-assistant` 对话内容走 LLM 本地 fallback；这是允许的降级，不影响 Go gRPC 上下文注入验证。
3. 服务日志中未发现 `using local SKU mock data`、`using local store mock data`、`using local snapshot mock data` 或 `All ... gRPC addresses failed`，说明 AI 到 Go 的 gRPC 通信正常。
4. 前端真实模式依赖 `localhost:8080` 统一入口；本轮使用临时网关完成转发。没有网关时，前端真实模式无法直接访问分散在 8081/8082/8083/8084 的后端服务。
## 追加联调记录：前端数据来源与 AI 对话门店编号

### 数据来源判定

当前如果前端启动时设置了 `VITE_USE_MOCK=false`，则前端不会使用 `AegisMockApi` 中的前端 mock 数据，接口会请求 `http://localhost:8080/api` 网关，再转发到对应后端微服务。

各类数据来源如下：

- 门店、SKU、用户、销售、库存、调拨等业务数据：来自 Go 后端微服务和 PostgreSQL 数据库中的种子/演示数据，以及运行过程中通过真实接口新增或修改的数据。
- AI 会话、AI 历史记录：来自 `ai-assistant` Python 微服务和 Python 侧数据库数据。
- 门店客流实时数据：当前未接入 YOLO 边缘节点时，由 `traffic-sense` 后端内置模拟器生成，并通过 WebSocket 推送给前端；这不是前端 mock。
- 后续 YOLO 边缘节点接入后，应由边缘节点调用 `traffic-sense` 的 `/edge/traffic/snapshot` 或 `/edge/traffic/history-batch`，再由云端 Python 服务推送给前端，并通过 gRPC 同步到 Go 门店业务服务。

判定方式：

- 浏览器 Network 中如果请求命中 `/api/...` 且目标为 `localhost:8080`，说明前端真实接口模式已生效。
- 如果页面不发真实 HTTP 请求，而直接显示数据，则需要检查前端启动命令是否遗漏 `VITE_USE_MOCK=false`。
- 如果客流数值持续变化但未接入 YOLO，这是 `traffic-sense` 后端模拟器数据，不是前端 mock，也不是 YOLO 实时识别结果。

### LT-001 AI 对话提示“未获取到门店编号”

问题编号：LT-001

模块：前端 AI 对话 / 总部端 `web-hq`

接口/页面：AI 对话页面，聊天 SSE 接口 `/api/ai/chat/completions`

严重程度：中

复现步骤：

1. 使用总部账号 `head001 / 123456` 登录总部端。
2. 进入 AI 对话页面。
3. 输入问题并发送。
4. 页面提示“未获取到门店编号”。

期望结果：

- 总部端 AI 对话可以选择一个目标门店后发起对话。
- 发送请求时应携带有效的正整数 `store_id`。
- 门店端账号应自动使用当前门店 `store_id`。

实际结果：

- 总部账号的 `store_id` 为 `0`，表示总部或非具体门店。
- 前端聊天页面使用 `!me?.store_id` 判断是否缺少门店编号。
- JavaScript 中 `0` 会被判定为 falsy，因此总部账号被误判为“未获取到门店编号”。
- 请求在前端被拦截，通常不会真正发送到后端 AI 接口。

初步原因：

- 该问题主要是前端页面逻辑/产品交互问题，不是 gRPC 通信问题。
- 后端 `/api/auth/me` 返回总部账号 `store_id=0` 属于当前数据设计。
- `ai-assistant` 的聊天接口要求 `store_id > 0`，因为 AI 分析需要明确门店上下文，所以不建议让后端直接接受 `store_id=0` 作为普通门店对话。

建议修复方式：

1. 短期联调 workaround：使用门店账号 `store001_mgr / 123456` 测试 AI 对话，门店账号有有效 `store_id`。
2. 总部端正式修复：在总部端 AI 对话页面增加“目标门店”选择器，从门店列表接口加载启用门店，用户选择后再调用 AI 对话接口。
3. 总部端发送 AI 请求时，不应直接使用 `me.store_id`，而应使用 `selectedStoreId`。
4. 前端判断门店编号是否缺失时，不应简单使用 `!me?.store_id`；门店端可改为显式判断 `me?.store_id == null`，总部端则必须先选择目标门店。
5. 后端保持 `store_id > 0` 校验；如果未来需要总部全局 AI 分析，应单独设计“全局分析”接口或参数，不要混用门店级聊天接口。

验证标准：

- 使用 `store001_mgr` 登录门店端，AI 对话可以正常发送，Network 中能看到 `/api/ai/chat/completions` 请求。
- 使用 `head001` 登录总部端，页面先选择具体门店，再发送 AI 对话，请求体中携带所选门店的正整数 `store_id`。
- 后端日志中不应出现因 `store_id=0` 导致的参数校验失败。
- AI 服务调用 Go gRPC 获取门店上下文时，不应 fallback 到本地 mock。
